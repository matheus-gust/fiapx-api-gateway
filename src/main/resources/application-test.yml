spring:
  datasource:
    url: jdbc:h2:mem:testgatewaydb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

jwt:
  secret: test-secret-key-for-testing-only-256-bits-long!!
  expiration-ms: 3600000
